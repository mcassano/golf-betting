# CLAUDE.md

## Keep README in sync

When changing `routes/`, `services/`, `server.js`, or adding/removing features, re-read `README.md` before finishing and update it if anything described there is now wrong, missing, or stale. Flag gaps to the user even if you don't fix them.
